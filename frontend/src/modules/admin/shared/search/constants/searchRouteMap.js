export const SEARCH_ROUTE_MAP = {

  movie:
    (id) =>
      `/admin/movies/${id}/edit`,

  theatre:
    (id) =>
      `/admin/theatres/${id}/edit`,

  person:
    (id) =>
      `/admin/persons/${id}/edit`,
}